(module
   "test/testcases/malgo/CopatternApplyChain.mlg"
   ((import "runtime/malgo/Builtin.mlg" all)
      (import "runtime/malgo/Prelude.mlg" all)
      (type F1 () (record (apply (-> Int32 (-> Int32 (-> Int32 Int32))))))
      (def
         addThree
         (codata
            ((project (apply (apply (apply # x) y) z) "apply")
               (apply (apply addInt32 (apply (apply addInt32 x) y)) z))))
      (def
         main
         (fn
            ((clause
                (_)
                (seq
                   (do
                      (apply
                         printString
                         (apply
                            toStringInt32
                            (project
                               (apply
                                  (apply (apply addThree (int32 1)) (int32 2))
                                  (int32 3))
                               "apply")))))))))))