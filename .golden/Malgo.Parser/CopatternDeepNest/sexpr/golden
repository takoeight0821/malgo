(module
   "test/testcases/malgo/CopatternDeepNest.mlg"
   ((import "runtime/malgo/Builtin.mlg" all)
      (import "runtime/malgo/Prelude.mlg" all)
      (type Inner () (record (val Int32)))
      (type Middle () (record (inner Inner)))
      (type Outer () (record (middle Middle)))
      (def
         nested
         (codata
            ((project (project (project # "middle") "inner") "val") (int32 42))))
      (def
         main
         (fn
            ((clause
                (_)
                (seq
                   (do
                      (apply
                         printString
                         (apply
                            toStringInt32
                            (project
                               (project (project nested "middle") "inner")
                               "val")))))))))))