# Malgo Language Design

malgoの構文・言語機能そのもの（コンパイラの実装ではなく、言語利用者が書く`.mlg`コードの見た目と意味）に関する語彙。Elixir/Rhombusのような可読性を目指す中長期の構文再設計を進める中で確定した用語を収める。

## Language

**コア構文糖 (core syntax sugar)**:
Elixirに倣い、if/case/cond相当の日常的な読みやすさを支える構文要素（レコード更新式、単一定義、ガード節、文字列補間など）。パーサ（`CStyle.lean`）の具象文法への直接変更として実装し、汎用マクロ機構を経由しない。
_Avoid_: 組み込み構文、ビルトイン構文

**マクロ (macro)**:
コンパイル時に構文木を別の構文木へ変換する、将来のユーザー拡張向けの別投資。既存の構文解析結果を入力とする構文木レベルのマクロとして設計し、Rhombusのshrubbery相当の、任意の中置演算子をトークン列から再構成するmixfix/enforestation機構は導入しない。コア構文糖の実装手段ではない——`CStyle.lean`は手書き再帰下降で文法が静的に固定されているため、マクロは既にパーサが解釈できる形の構文にしか意味を後付けできず、新しいトークンや記法（`..`によるスプレッド、`when`によるガードなど）の導入には向かない。
_Avoid_: シュラバリー、マクロ展開器

**オプショナルインデント (optional-brace indentation)**:
`let`・`with`・節の`->`など、既にキーワード/演算子でアンカーされた位置に限り、インデントを`{ }`の代替記法として使えるようにする字句解析器の拡張。Scala 3のオプショナルブレースに倣い、明示的な`{ }`とインデントは完全に互換で混在できる。`if`/`unless`/`cond`のようなキーワードを持たない関数呼び出しのサンク引数は対象外。
_Avoid_: レイアウト規則（Pythonのような、常にインデントを強制する方式と混同しない）

**サンク/遅延ブロック (thunk block)**:
`{ expr }`という記法が、パターンなし・単一節の関数リテラル（内部的には`{ _ -> expr }`と同義）として遅延評価される値。`if`/`unless`/`cond`はこの仕組みを使って`Bool -> {a} -> {a} -> a`のような通常のPrelude関数として実装されており、コンパイラの特別構文ではない。関数定義の複数節（`{ p1 -> e1, p2 -> e2 }`）やletの逐次実行ブロック（`{ let x = ...; ... }`）も同じ`{ }`構文の別の使い方であり、区別は今のところ視覚的なものでしかない。

**ガード節 (guard clause)**:
パターンに加えて任意の真偽式による絞り込みを節に付与する記法（`pattern when cond -> body`）。既存の`cond : List (Bool, {a}) -> a`が持つ「タプルリスト+`True`番兵」という呼び心地の悪さを解消し、多方向分岐を既存の節構文の延長として書けるようにする。

**レコード更新式 (record update expression)**:
既存のレコード値から一部のフィールドだけを上書きした新しいレコード値を作る式。元のレコードの残りのフィールドを引き継ぐスプレッド構文（`{ ..r, .field -> v }`）を採る。
_Avoid_: レコードマージ（複数レコードの結合を指す別概念と混同しない）

**単一定義 (unified def)**:
型注釈と実装を1つの`def`にまとめた関数定義の書き方。現在は型注釈用の`def`と実装用の`def`が別々に必要で、キーワードが重複している。

**文字列補間 (string interpolation)**:
文字列リテラルの内部に式を埋め込む記法。`appendString`のネストによる文字列組み立てを置き換える。文字列リテラルの字句解析そのものへの変更が必要。
